; Copy this file to `.env` and fill in real values.
; These should be base64-encoded strings (no surrounding whitespace).
ENCRYPT_FIRST_KEY = "REPLACE_ME_BASE64"
ENCRYPT_SECOND_KEY = "REPLACE_ME_BASE64"

